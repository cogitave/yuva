# Yuva Forward Backlog

> Prioritized, dependency-ordered task plan produced by an ultracode mine+synthesize workflow (2026-06-08), after the v2 agent-native chain M5–M18 went fully CI-green and the L2 sovereignty track started at L2.0. Raw: [research/raw/forward-backlog.json](../research/raw/forward-backlog.json).
>
> **Detailed per-task implementation plans:** [plans/INDEX.md](plans/INDEX.md) — a second ultracode workflow (2026-06-08) authored a research-backed, decision-closed plan for the highest-priority forward tasks (verification, L2 sovereignty, IPC, aarch64 EL2), with a synthesized critical-path / parallel-track view. Each plan there carries its own DoD marker, file list, decisions, verification steps and research citations.
>
> **#1 (Fix M8 LAPIC/timer fault under QEMU-microvm+KVM) is DONE** (2026-06-08, commits `80e6ce3` + `2de01c2`): root cause was `lapic_enable()` leaving the non-timer LVTs unmasked, so the first `sti` took a stray ExtINT → `#DE` on `microvm` (no 8259 PIC). The microvm+KVM lane now reaches `M18: evolve OK`. See [`tabos-overnight-autonomous-run`] memory + the l2-nested-vmx CI lane.

## Summary

Forward task backlog for Yuva (from-scratch no_std Rust framekernel) synthesized from mined deferred-hooks, the L2 sovereignty chain, verification gaps, portability bugs, perf/bench, and docs debt. The v1 (M0-M4) + v2 agent-native chain (M5-M18) are complete and CI-green (TCG + tb-vmm/KVM); the L2 microhypervisor track has started at L2.0 (VMX-root, builds + boots via a graceful-skip path that proves nothing about the world switch). Ordering is leverage-first: (1) the F2 M8 LAPIC/timer fault under QEMU-microvm+KVM is a real boot bug that also unblocks the KVM bench full number; (2) the F1 nested-VMX validation substrate gates every L2.0-L2.6 real hardware-virt proof since hosted CI has no nested VMX; (3) the M11 rights-subset/confused-deputy formal proof is load-bearing for M18's entire frozen-boundary safety guarantee. These three are P0. Then Miri-in-CI (mandatory Tier-0) and the L2.0 real VMLAUNCH proof, then the L2.1..L2.9 chain in strict dependency order (each its own task), the aarch64 tb-boot/EL2 parallel track (CI-friendly TCG world-switch proof), then the deferred features (byte-payload IPC, blocking-recv IPC, durable VirtioBlkStore, real inference adapters + virtio transport, M18 human-approval gate, M18 held-out evaluator, M15 unmap/reclaim), broader Kani harnesses, and finally perf-bench + docs reconciliation. Grounded against crates/tb-hal/src/arch/x86_64/vmx/ (controls/ept/exit/vmcs/vmxon/world_switch.rs), timer.rs (LAPIC at PML4[3] VA 0x0000_0180_0000_0000 -> PA 0xFEE0_0000), caps.rs (args[2..3] reserved buffer + rights-subset invariant doc), ipc.rs (copy_to/from_user deferred + WouldBlock), mem.rs (BackingStore/RamStore, VirtioBlk deferred), scripts/bench-boot.sh (FINAL_MARKER='L2.0: vmxroot OK', -M microvm full=NA), and .github/workflows/{ci,vmm-boot,l2-nested-vmx}.yml. 30 tasks.

## Tasks (leverage-ordered)

| # | Priority | Track | Task | DoD | Depends on | Effort |
|---|---|---|---|---|---|---|
| 1 | **P0** | portability-bug | Fix M8 LAPIC/timer fault under QEMU-microvm+KVM (F2) | Kernel boots M0->M18 and streams the final cumulative marker under `qemu-system-x86_64 -M microvm -accel kvm -cpu host`; no trap at cause=0x0 fault_addr=0x0 ~pc=0x1311ff during M8 bring-up. Root-caused in crates/tb-hal/src/arch/x86_64/timer | none | M |
| 2 | **P0** | infra | Stand up a nested-VMX validation substrate (self-hosted or bare-metal lane) (F1) | A CI/test lane exists where vmx is present in /proc/cpuinfo and kvm_intel nested=1 is loaded (self-hosted nesting runner OR a documented bare-metal procedure on Arda's hardware), capable of running -cpu host,+vmx so VMXON/VMLAUNCH/EPT/exit  | none | M |
| 3 | ✅ DONE | verification | Prove M11 capability rights-subset / no-confused-deputy invariant with Kani/Verus (task #38: extracted the Rights algebra + generic `CapTable<O>` into `crates/tb-caps-core`, added 12 Kani harnesses + the `.github/workflows/kani.yml` lane; DoD marker `M11: caps-subset PROVEN`) | A Kani (or Verus) harness in-tree proves capability-derivation monotonicity: across every {mint, dup, narrow, transfer, revoke} sequence the rights of any resolvable handle are a SUBSET of its parent's, and no derivation closure can reach a | none | L |
| 4 | **DONE** | verification | Wire Miri into CI as a mandatory Tier-0 gate | LANDED (`T0: miri OK`, new `.github/workflows/miri.yml`): `cargo miri test -p tb-caps-core -p tb-encode` interprets the EXACT pure host-runnable leaf crates the kernel runs (the same code Kani verifies — zero model drift) under the MIR interpreter, checking every path for UB (OOB, use-after-free, uninit reads, invalid enum/bool, misalignment, integer overflow via overflow-checks, strict-provenance). tb-hal + kernel are excluded (asm + os=none triple Miri cannot execute; `-p` never `--workspace`). 18 host #[cfg(test)] tests added — esp. the tb-encode `MessageFrame::decode` untrusted-byte parser over every length × fill pattern, BoundedRing overflow/wrap, and the tb-caps-core CapTable alloc/dup/narrow/transfer/revoke/generation-overflow sequences. Fail-closed: any UB or failing test makes `cargo miri test` exit non-zero before the marker echoes (no `|| true`/continue-on-error). Verified locally (orchestrator re-ran it): 11 + 19 tests pass under Miri, 0 UB. Kernel-inert: tb-caps-core lib.rs `#![no_std]`→`#![cfg_attr(not(test), no_std)]` (test-only; both arches still kbuild 0-warnings, Kani still inert). LANGUAGE-AND-STANDARDS §5 / OPEN-Q §I. | none | M |
| 5 | **P1** | infra | Add QEMU-microvm+KVM as a third CI boot lane | A CI job boots the kernel under `-M microvm -accel kvm -cpu host` and asserts the M18 cumulative marker; emits a comparable boot-ready number alongside ci(TCG) and vmm-boot(tb-vmm/KVM). Lane is green and required. | Fix M8 LAPIC/timer fault under QEMU-microvm+KVM (F2) | S |
| 6 | **P1** | L2-sovereignty | Execute the L2.0 real VMLAUNCH proof (Proven exit_reason=10) | On the nested-VMX substrate, the VMXON->VMCS->EPT->VMLAUNCH->caught-exit path runs the REAL (non-skip) flow and emits Proven{exit_reason:10} (CPUID forced-exit); the graceful-skip branch is bypassed. Marker 'L2.0: vmxroot OK' is backed by a | Stand up a nested-VMX validation substrate (self-hosted or bare-metal lane) (F1) | M |
| 7 | **P1** | L2-sovereignty | Implement L2.1 EPT-violation demand handling (ept OK) | A trivial guest touches an unmapped GPA; tb-core catches exit reason 48, reads the guest-physical-address, maps a frame, issues INVEPT, and resumes; EPT-misconfig (49) treated as fatal. Serial marker 'ept OK' on the nested-VMX lane. | Execute the L2.0 real VMLAUNCH proof (Proven exit_reason=10) | L |
| 8 | **P1** | L2-sovereignty | Implement L2.2 full MUST exit set (exits OK) | tb-core handles the §4 MUST exit set: CPUID forge, RDMSR/WRMSR via MSR bitmap, CR-access (28), HLT (12), triple-fault teardown (2), VMCALL (18) as the hypercall entry; RIP-advance discipline enforced (advance after trapping exits, never aft | Implement L2.1 EPT-violation demand handling (ept OK) | L |
| 9 | **P1** | L2-sovereignty | Implement L2.3 device-model seam and extract crates/tb-core (devmodel OK) | An I/O-exit (reason 30, COM1 0x3f8) is forwarded to a deprivileged 16550 callback that prints; the tb-core<->device-model EXIT-UPCALL + vcpu_run-returns-exit-reason ABI is defined; crates/tb-core is created as a no_std #![forbid(unsafe_code | Implement L2.2 full MUST exit set (exits OK) | XL |
| 10 | **P1** | L2-sovereignty | Implement L2.4 nested Yuva M0-M18 guest boot (yuva-guest OK) | tb-core boots the REAL unmodified Yuva M0-M18 kernel as its nested guest using the same tb-boot v0 register file written into VMCS guest-state plus the device-model console; nested M-markers are forwarded out. Confirms ZERO guest changes.  | Implement L2.3 device-model seam and extract crates/tb-core (devmodel OK) | L |
| 11 | **P1** | L2-sovereignty | Implement L2.5 sovereign VMX-preemption-timer scheduling (sched OK) | The VMX-preemption timer (exit reason 52) drives a tb-core scheduler tick; >1 vCPU is time-sliced under tb-core ownership; VMX_MISC preemption-timer scale factor is correctly applied. Serial marker 'sched OK'. | Implement L2.4 nested Yuva M0-M18 guest boot (yuva-guest OK) | M |
| 12 | **P2** | L2-sovereignty | Implement L2.6 SMP / AP bring-up (smp OK) | INIT-SIPI-SIPI + APIC AP startup brings up >1 physical CPU; per-pCPU VMXON + per-vCPU VMCS established; the guest runs across multiple physical cores. Serial marker 'smp OK'. Closes OPEN-Q §J 'type1-x86-vmx' SMP gap. | Implement L2.5 sovereign VMX-preemption-timer scheduling (sched OK) | XL |
| 13 | **P2** | L2-sovereignty | Build the firmware-baremetal platform body (ACPI/PCIe-ECAM/APIC/timer-cal) | A platform crate/module parses ACPI (RSDP, MADT, MCFG), walks PCIe ECAM, programs the APIC, and calibrates the timer, suitable for use after ExitBootServices; unit-tested against captured tables. Tracked as its own milestone (closes the 'un | none | XL |
| 14 | **P2** | L2-sovereignty | Implement L2.7 UEFI bare-metal Type-1 launch (baremetal OK) | tb-core runs as an EFI app (uefi-rs): captures the UEFI memory map + ACPI RSDP + GOP framebuffer, calls ExitBootServices, installs in VMX-root with NO host kernel, builds EPT, and boots Yuva from bare metal. Serial/framebuffer marker 'bare | Implement L2.4 nested Yuva M0-M18 guest boot (yuva-guest OK), Build the firmwa | XL |
| 15 | **P2** | L2-sovereignty | Implement L2.8 VT-d IOMMU DMA confinement (iommu OK) | DMAR parsed defensively (refuse passthrough on malformed table/RMRR per Xen posture); root/per-bus context tables + second-level tables mirroring each guest's EPT + mandatory VT-d interrupt remapping built; a passed-through device's DMA is  | Implement L2.7 UEFI bare-metal Type-1 launch (baremetal OK) | XL |
| 16 | **P2** | L2-sovereignty | Implement L2.9 full split-VMM with <10K-LOC TCB budget (split-vmm OK) | The complete tb-vmm device model is demoted to a deprivileged guest; tb-core forwards ALL MMIO/PIO exits over the hypercall/shared-ring ABI and contains ZERO device emulation; the KVM-facing spine (memory.rs, vmm.rs run loop, arch/boot.rs)  | Implement L2.4 nested Yuva M0-M18 guest boot (yuva-guest OK) | XL |
| 17 | **PARTIAL (host-green)** | portability-bug | Build aarch64 tb-boot producer + _tb_start EL1 entry | LANDED the host-verifiable contract: `crates/tb-boot/src/aarch64.rs` producer (`build_handoff` writes TbBootInfo + a TbMemRegion[] + cmdline into guest RAM @0x4000_0000 and the KVM_SET_ONE_REG register file: X0=TbBootInfo*, X1..3=0, PC=entry, PSTATE=0x3c5 EL1h+DAIF, mirroring the x86 producer), and the `_tb_start` aarch64 EL1 entry (a SEPARATE `.globl` symbol that stashes X0, sets BOOTED_AT_EL2=0, and branches to the shared EL1 continuation — additive, the existing x0=FDT `_start` + #48 EL2 drop are byte-identical; e_entry stays `_start`). A `.note.TABOS` PT_NOTE (linker PHDRS added) carries `_tb_start`'s address for a future loader. 8 new host #[cfg(test)] round-trip tests (producer→serialize→parse recovers the same TbBootInfo) — `cargo test -p tb-boot` 22 passed, tb-vmm 26 passed. Both arches build 0-warnings + boot the full chain (aarch64 `el2 OK` via the FDT path intact). DEFERRED (needs Arda / aarch64 KVM): the tb-vmm aarch64 KVM_ARM backend (VCPU_INIT, SET_ONE_REG, vGIC, PL011) that CONSUMES the handoff + boots through `_tb_start` → the `tb-boot: contract v0 OK` serial marker; + the kernel-side aarch64 TbBootInfo consumer (read_boot_magic window widen + pmm). No aarch64 /dev/kvm in CI. | none | L |
| 18 | **P1** | L2-sovereignty | Implement aarch64 EL2 (nVHE) world-switch smoke proof (el2 OK) under TCG | el2.rs (HCR_EL2/VTCR_EL2/VTTBR_EL2 + EL2 vectors + ERET) and stage2.rs (IPA->PA + TLBI VMALLS12E1) bring up an EL2->EL1 world switch under QEMU virt `-machine virtualization=on -cpu max` in PURE TCG; serial marker 'el2 OK' on a stock hosted | Build aarch64 tb-boot producer + _tb_start EL1 entry | L |
| 19 | **DONE** | verification | Author Kani harnesses for silicon-unsafe encoders and protocol parsers | LANDED (`V1: kani-encoders OK`, new `prove-encode` CI job): CRATE-LAYERING DECISION made — the PURE bit-algebra entangled with tb-hal's silicon-unsafe I/O was extracted into a NEW `crates/tb-encode` (no_std, #![forbid(unsafe_code)], host-buildable; the `tb-core` name stays reserved for the future L2 orchestrator #44). tb-hal now CALLS tb-encode (value-compute moves out; the vmwrite/read_volatile/asm stays). 11 Kani harnesses prove: the control-MSR adjust legality gate (the #1 cause of silent VM-entry failure) — forces all allowed-0 bits, clears all non-allowed-1 bits, under the Intel allowed0⊆allowed1 well-formedness precondition; CR0/CR4 fixed-bit clamp; TSS-base decode vs an independent re-derivation; page-table/EPT entry encoders (address preserved, flags preserved, level-index<512, EPT leaf/non-leaf/EPTP well-formed); the 16-byte IPC frame round-trip + total fail-closed decode; a bounded no-alloc ring. 12 host #[cfg(test)] tests assert byte-identical-to-legacy behavior. Behavior-preserving: both arches build 0-warnings + boot the full M1..L2.0 chain. Adversarial verification caught + fixed two over-quantified harnesses (missing well-formedness assumes). tb-caps-core M11 proof untouched (12 harnesses); the new prove-encode job is independent + fail-closed (0 FAILED & SUCCESSFUL==11). | Prove M11 capability rights-subset / no-confused-deputy invariant with Kani/Verus (#38, done) | L |
| 20 | **DONE** | deferred-feature | Implement M14 byte-payload copy_to_user/copy_from_user IPC | LANDED (`M14.1: payload OK`, both arches): variable-length BYTE payloads transfer via a kernel-heap bounce buffer (`ipc::MAX_PAYLOAD=4096`) filled by a sender-side `copy_from_user` and drained by a receiver-side `copy_to_user`; the two raw copy primitives (software page-table walk vs an explicit root + byte copy through the kernel supervisor identity alias, SMAP/PAN-immune) are the single deliberately-deferred M14 unsafe, now confined to NEW per-arch `arch/{x86_64,aarch64}/uaccess.rs` modules; `ipc.rs`/`caps.rs`/kernel stay zero-unsafe behind the safe facade. Fail-closed (oversize -> Denied, copy-fault/too-small -> Fault, no message loss). The EL0 user-syscall path plumbing `args[2..3]`=(uva,len) through the register-lift shim is the additive layer, reserved with blocking-recv as `M14.2` (row 21). | none | M |
| 21 | **DONE** | deferred-feature | Implement M14 blocking-recv / runnable-on-send scheduler<->IPC integration | LANDED (`M14.2: blocking-recv OK`, both arches): a receiver on an empty channel registers a per-side waiter and parks OFF the M9 run queue (TASK_STATE BLOCKED, schedule() skips it); a sender's push wakes it (runnable-on-send) from both M_CHAN_SEND and chan_send_bytes. Lost-wakeup-free via a single masked critical section (waiter registered before yield); proven with the timer ARMED (real preemptive path), boot completes in ~15s with no hang. WouldBlock/PeerClosed preserved. | none | M |
| 22 | **DONE → M20** | deferred-feature | Implement M13 durable VirtioBlkStore BackingStore | LANDED as a NEW milestone **M20** (`M20: persist OK`, both arches, real durability round-trip): `VirtioBlkStore: BackingStore` (mem.rs) drops in behind the existing RamStore seam via the new `MemSubstrate::new_with_backing`. Backed by a poll-only modern **virtio-blk** (DeviceID 2) driver added to `arch/{x86_64,aarch64}/virtio.rs` (reuses every M19 primitive; the only new surface is the config-space capacity read + a 3-descriptor request chain header/data/status, `Q_SIZE=4`). On-disk is **log-structured**: a 512-byte FNV-1a-64-checksummed superblock at LBA 0, append-only per-Region record frames over a fixed >=4 MiB partition. `flush()` is a **two-phase commit** (dirty records → VIRTIO_BLK_T_FLUSH → superblock at gen+1 → FLUSH); `mount()` validates fail-closed, formats fresh disks, and replays each Region log up to the committed watermark (torn tail ignored). `epoch()=(gen<<32)|appends_since_mount` is monotonic across reboots. ALL value computation (req-header/superblock/record-frame/episode codecs + sector math) lives in the NEW `tb-encode::blkfmt` leaf, Kani-proven (6 new harnesses; verify-encode.sh 28→34); ALL MMIO/DMA stays in `arch/*/virtio.rs`; the kernel branches on a pure-data `PersistProof`. The selftest writes N sentinels through a real Region, two-phase-flushes, DROPS the substrate, RE-MOUNTS the same disk, replays, and asserts replayed==written + gen bumped by 1. **Graceful skip** `(no disk, skipped)` where no `-drive` is attached (tb-vmm/vmm-boot stay green, unchanged). Crash-consistency scoped to clean-flush durability (the commit point); crash-at-an-arbitrary-point is a named non-claim. | Implement M16 virtio transport + tb-vmm host-side device backend | L |
| 23 | **PARTIAL → M19** | deferred-feature | Implement M16 virtio transport + tb-vmm host-side device backend | LANDED the in-kernel transport as a NEW milestone **M19** (`M19: virtio OK`, both arches, real device I/O): a poll-based **virtio-mmio** driver in NEW `crates/tb-hal/src/arch/{x86_64,aarch64}/virtio.rs` does a full **virtio-rng** round-trip — arch-neutral slot scan (MagicValue/DeviceID; x86 base 0xFEB00000 UC-mapped at `LAPIC_WINDOW_VA+0x1000`, aarch64 base 0x0A000000 already Device-mapped), modern (Version=2) handshake, one queue (QueueNum=1) with desc/avail/used + buffer in ONE identity-mapped `frame_alloc` frame, submit→QueueNotify→**poll `used.idx`** under a fail-closed `POLL_CAP=100M` (never hangs), aarch64 dmb/dsb DMA barriers, `NO_INTERRUPT` set. Safe `virtio_selftest()->VirtioProof{Absent|LegacyUnsupported|Proven|Failed}` facade (mirrors VmxProof/El2Proof); kernel zero-unsafe. **Graceful skip**: under tb-vmm (no device) the UC read returns open-bus 0xFFFFFFFF → Absent → `M19: virtio OK (no device, skipped)`, so vmm-boot stays green with NO tb-vmm backend. ci/microvm-kvm lanes pass `-device virtio-rng-device -global virtio-mmio.force-legacy=false`. DEFERRED to follow-ups: the completion-IRQ path (aarch64 GIC SPI 48 first, then an x86 IOAPIC driver), the M14-stream integration, and the tb-vmm host-side virtio backend. **M30 update (stages A+B landed):** the in-kernel transport grew its SECOND consumer class — the M30 verified inference-transport channel (a modern virtio-console, DeviceID 3, the kernel's first TWO-queue setup, poll-only) with the `xport-harness` host echo peer on the QEMU chardev lanes; the tb-vmm host-side device backend REMAINS the deferred piece — it is M30 stage C (proposal §3d/§11C, `transport=TB-VMM-HOST`), pre-authorized as its own follow-up landing. | none | L |
| 24 | **P2** | deferred-feature | Implement M16 real inference adapters (Anthropic/OpenAI HTTP + vsock GPU) | Anthropic + OpenAI HTTP backends and a vsock-local GPU/CUDA driver-VM adapter all implement the existing InferBackend trait (infer.rs) and route via the longest-prefix model: scheme (plus the deferred preference-vector scoring if landed); a | Implement M16 virtio transport + tb-vmm host-side device backend | L |
| 25 | **DONE** | deferred-feature | Implement the M18 mandatory human-approval gate for EMIT_EXTERNAL/high-impact skills | LANDED (`M18.1: approval-gate OK`, both arches): new right `APPROVE_HIGH_IMPACT` (bit 12) in tb-caps-core; skills proposed via op `ADD_SKILL_EMIT_EXTERNAL` carry a `SKILL_PROV_EMIT_EXTERNAL` provenance bit; the merge step (`harness_merge`) is fail-closed for that class — absent / insufficient / forged approval capability → `Denied` (the skill stays PROPOSED), admitted only when a presented cap carries `APPROVE_HIGH_IMPACT`; ordinary skills merge unchanged. Self-test proves all four cases (no-approval Denied, insufficient-cap Denied, real-cap Admitted, ordinary Admitted). The guarantee reduces to the Kani-proven M11 rights-mask invariant. SELF-IMPROVEMENT-SPEC §8. | Prove M11 capability rights-subset / no-confused-deputy invariant with Kani/Verus (#38, done) | M |
| 26 | **DONE** | deferred-feature | Implement M18 rotating held-out evaluator partition | LANDED (`M18.2: held-out OK`, both arches): generalizes the single frozen evaluator to N=3 held-out SCORING partitions + 1 VISIBLE partition, all minted in the kernel-owned `eval_tbl` (never in any agent table → unnameable/unreadable/unwritable, reducing to the Kani-proven M11 rights-mask invariant). A kernel-side rotation counter the agent cannot observe or influence picks `idx = rotation % N`; the scoring set excludes the visible partition by construction, so the scorer is never the visible one. Anti-Goodhart: the visible metric and the hidden rotated scorer rank candidates oppositely, so a candidate tuned to the visible metric is REJECTED by the real scorer (stays PROPOSED) while the scorer-matched candidate is ADMITTED. Self-test proves all three properties fail-closed (>1 unnameable partitions, unobservable rotation, visible↔scoring decoupling). No new Rights bit / no new unsafe; caps.rs forbid-unsafe, kernel zero-unsafe; tb-caps-core untouched (12 Kani harnesses intact). | Prove M11 capability rights-subset / no-confused-deputy invariant with Kani/Verus (#38, done) | M |
| 27 | **DONE** | deferred-feature | Implement M15 M_BLOCK_UNMAP + frame reclamation | LANDED (`M15.1: unmap OK`, both arches): owner-only `M_BLOCK_UNMAP` (requires `Rights::REVOKE`) tears down EVERY recorded member mapping in EVERY root (clear leaf PTE + LOCAL TLB invalidate — x86 invlpg / aarch64 Break-Before-Make tlbi), poisons the shared `Rc<Block>` core (every outstanding handle in any table → Stale, the channel peer-closed idiom), generation-bumps the caller's slot, then `frame_free`s the data frames — three UAF locks before any frame is reclaimed. Frames are no longer pinned for the session lifetime. Self-test proves fail-closed (member-without-REVOKE → Denied, non-block → BadCap), reclamation (pmm free-count +1 and the allocator hands the block PA back out), and no-stale-access (revoked/poisoned handles Stale + a safe page-walk shows the old VA maps nowhere); the whole chain M16..M18.1 + L2.0 stays green after (no double-free/allocator corruption). New unsafe (unmap_in_root / va_to_pa_in_root) confined to arch/*/mmu.rs; kernel zero-unsafe; blocks.rs/caps.rs forbid-unsafe. | none | M |
| 28 | **P1** | perf-bench | Replace the BENCHMARKS Yuva placeholder with a measured release boot-ready-cycles number | A measured --release boot-ready-cycles figure (in-guest rdtsc / CNTPCT_EL0, from the vmm-boot KVM job) replaces the 'Bucket 1 fast end (target)' / confidence '—' row in BENCHMARKS.md §3, converting target->measured against the Unikraft/Mira | none | S |
| 29 | **P2** | perf-bench | Harden bench-boot.sh fast-KVM marker capture (n=4/20 -> reliable) | Under fast KVM the FINAL_MARKER ('L2.0: vmxroot OK') is captured on >=18/20 runs (currently ~4/20) via a robust file-poll/settle in bench-boot.sh; n_full reported by the harness is no longer dominated by reader-flake drops. | Fix M8 LAPIC/timer fault under QEMU-microvm+KVM (F2) | S |
| 30 | **P1** | docs | Reconcile MILESTONES.md + ARCHITECTURE.md with the M5-M18 chain and L2 track | docs/MILESTONES.md and docs/ARCHITECTURE.md describe the completed M5-M18 agent-native chain and the L2 tb-core sovereignty narrative (L0/L1 Firecracker-guest framing updated to include the Type-1 L2 story); ROADMAP-V2 §6 status table stays | none | M |
